import { counterSlice } from "@/redux/counter/counter-slice"
import { customerSlice } from "@/redux/customers/customer-slice"

const allSlices = [counterSlice, customerSlice]

export default allSlices
